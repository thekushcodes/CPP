#include <iostream>
using namespace std;

// C++ Templates : Must for Competetive Programming
/*
Class is a template for an object.
Class ke lie template is Template.
Templates->> Parameterized classes(Classes having parameters)
Template- Class ko ek baar bnaunga aur alg alg data types ke lie classes apne aap bn jaengi

Why use Templates?
-> Supports DRY principle.
-> We can do generic programming(not specific for any particular data type).

Syntax for Template:
The below class code does not remain a class anymore, it represents several classes.
template <class T>
class vector{
    T* arr; // Array of whatever data type supplied in class. // T can be any data type like int, char, etc.
public:
    vector(T* arr){
    //code
    }
    };


int main(){
    vector<int>myvec(ptr);
    vector<float>myvec(ptr1);

    return 0;
}

*/

int main()
{

    return 0;
}