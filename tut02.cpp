#include <iostream>
using namespace std;

int main()
{
    cout << "Hello World";
    return 0;
}

/*
SYNTAX=>> #include<____>
#include<iostream>
iostream named file is already there, we can use things here from there. This is header.
io helps in input output. cout, cin are its functions.

'main()' is a function. It is the entry point. Program starts from main function.
() states that this is a function.
{} states that this is function body.

'std::cout' means that 'iostream' me ek function h cout aur ye function 'standard namespace' ke andar h.
Instead of writing like this(std::), we can write "using namespace std;" in the line before- int main() (In the header).

"" ke andr anything = str

Now we sent return 0 to main(). 0 return value means successful termination of program.

*/