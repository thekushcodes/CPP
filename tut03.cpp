/* Variables are containers that store any data. Different data types gets stored in different forms of variables like int, float, char, double, boolean (others are also there like long int, long double, signed char...).
char is like 'N','o' etc.
double is just like float which is used for more precision, like more number after decimal.
boolean is either True or False.
int a = 5
*/
// Comments are made like:
// This is a single line comment
/* This a multi-
line comment */

// 'ctrl plus /' is shortcut to comment.
#include <iostream>
using namespace std;

int main()
{
    int sum = 6; // This is variable named 'sum'
    cout << "Hello world" << sum;
    return 0;
}